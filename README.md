#server start dev (im ordner workspaces/myWebsite/myWebsite)
python manage.py runserver

#bei problemm mit dem runtime server oder so die befehle ausprobieren
python -m venv venv 
source venv/bin/activate
pip install django